--- a/src/main/java/io/papermc/paper/plugin/manager/PaperPluginInstanceManager.java
+++ b/src/main/java/io/papermc/paper/plugin/manager/PaperPluginInstanceManager.java
@@ -238,11 +_,31 @@
             plugin.getLogger().info("Disabling %s".formatted(pluginName));
 
             this.server.getPluginManager().callEvent(new PluginDisableEvent(plugin));
+            // SurfCanvas - Wrap setEnabled(false) in a thread with a 60s timeout to prevent hanging plugins from blocking world save
             try {
-                javaPlugin.setEnabled(false);
-            } catch (Throwable ex) {
-                this.server.getLogger().log(Level.SEVERE, "Error occurred while disabling " + pluginName, ex);
+                final Thread disableThread = new Thread(() -> {
+                    try {
+                        javaPlugin.setEnabled(false);
+                    } catch (Throwable ex) {
+                        this.server.getLogger().log(Level.SEVERE, "Error occurred while disabling " + pluginName, ex);
+                    }
+                }, "Plugin Disable - " + pluginName);
+                disableThread.setDaemon(false);
+                disableThread.start();
+                disableThread.join(java.util.concurrent.TimeUnit.SECONDS.toMillis(60));
+                if (disableThread.isAlive()) {
+                    this.server.getLogger().log(Level.SEVERE, "Plugin " + pluginName + " did not shut down within 60 seconds, interrupting");
+                    disableThread.interrupt();
+                    disableThread.join(java.util.concurrent.TimeUnit.SECONDS.toMillis(5));
+                    if (disableThread.isAlive()) {
+                        this.server.getLogger().log(Level.SEVERE, "Plugin " + pluginName + " is still running after interrupt, continuing with shutdown");
+                    }
+                }
+            } catch (InterruptedException ex) {
+                this.server.getLogger().log(Level.SEVERE, "Interrupted while waiting for plugin " + pluginName + " to disable", ex);
+                Thread.currentThread().interrupt();
             }
+            // SurfCanvas end
 
             // Flush async appender before unloading, avoids issues when a log message with class context
             // tries to print after it's source has unloaded (i.e., Guice enhanced errors)
